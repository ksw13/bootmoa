<?xml version="1.0"?>
<thlogic>
    <attr sel="#header" th:replace="header :: header" />
    <attr sel="#footer" th:replace="footer :: footer" />


    <attr sel="#article-form" th:action="'/articles/form'" th:method="post">
        <attr sel="#cancel-button" th:onclick="'history.back()'" />
    </attr>
</thlogic>
